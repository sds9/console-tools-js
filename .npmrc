# Specifies the npm registry URL to use for installing and publishing packages
# This is the standard public npm registry
registry=https://registry.npmjs.org/

# # When installing packages, save exact versions in package.json (e.g. 1.2.3 instead of ^1.2.3 or ~1.2.3)
# # This prevents unexpected updates and ensures consistent installations across environments
# save-exact=true

# # Enforces the "engines" field in package.json
# # If your Node.js version doesn't match what's specified there, npm will fail
# engine-strict=true

# # Ensures package-lock.json is always generated and used
# # This guarantees dependency tree consistency across installations
# package-lock=true